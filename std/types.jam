/** import_parameters {
    "set": [ "__typecheck", "types" ]
} */

let __typecheck = import "checks";

let types = { };

types.generate_from_prototype = type_name: string => arg => ((type set) == "table") && arg.__type && (arg.__type == type_name);

=> tbl (__typecheck, types);
