/** import_parameters {
    "set": [ "table" ]
} */

use "std/types.jam";

let table_mt = { 
    push: (t: table, v) => >> t #(len t + 1) = v;
    set: (t: table, k: exists, v) => >> t #k = v;
};

=> tbl table_mt;
